node_modules/
dist/
.env
.env.attestation
.env.base-leftover
*.log
.env.attestation*
.env.cdp-settlement*
# GATEWAY_HMAC_SECRET for the receipts payment-context wire contract (Plan-1
# Week-2) — never tracked; the repo is PUBLIC. Founder-provisioned only.
.env.receipts*
# x402-list.com domain-proof token (host-specific, expiring; served by /.well-known/x402list.txt)
deploy/base/x402list.txt

# Build-artifact backups (e.g. dist.pre-fixa-2026-07-28.CONTAINS-P1-BUG.bak/).
# The existing "dist/" rule does not cover these; this repo is PUBLIC and they are
# known-buggy compiled output with no reason to ship.
dist.*.bak/
